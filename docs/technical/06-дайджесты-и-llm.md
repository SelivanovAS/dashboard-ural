# 06. Дайджесты и LLM

## Что это и зачем

Дайджест — это то ради чего всё затевалось: ежедневная читаемая сводка, которую
юрист получает в Telegram и push. В ней — новые дела, изменения по делам, переходы
по инстанциям и, главное, **человеческий пересказ судебных решений**: что суд
постановил и чем это грозит банку.

Ключевая идея архитектуры: **HTML дайджеста собирается программно**
(детерминированно, из накопленных за прогон событий), а нейросеть (LLM)
вызывается только на одну узкую задачу — пересказать мотивировочную часть акта
в 2–3 предложения. Так дешевле и надёжнее: LLM не может «выдумать» несуществующее
дело или переврать цифры в сводке, потому что цифры и структуру делает код, а не LLM.

## Три режима генерации (флаги)

`generate_digest` ([368](../../scripts/court_monitor/digest/core.py#L368)) — точка входа,
которая выбирает режим:

| Режим | Когда | Как работает |
|-------|-------|--------------|
| **Гибридный** (по умолчанию) | всегда, если не задан `DIGEST_FULL_LLM` | `generate_template_digest` рендерит HTML + `summarize_act_motivation` пересказывает акты через LLM. |
| **+ Полировщик** | `DIGEST_POLISH=1` | Готовый черновик дополнительно прогоняется через `polish_digest_html` (косметика) с валидатором и откатом. |
| **Полный LLM** | `DIGEST_FULL_LLM=1` | Старое поведение: весь HTML генерирует один большой LLM-вызов. Escape hatch для A/B и на случай регресса. |

**С 03.07.2026 гибрид — боевой режим**: флаг `DIGEST_FULL_LLM` снят из
`replay_on_push.yml` и `update_cases.yml` после A/B-сравнения (юрист выбрал
компакт-вёрстку, покрытие всех 29 типов событий закреплено матрицей тестов
`tests/test_digest_template_events.py`). Откат — вернуть флаг в env workflow.

После отправки дайджест проверяет **программный линтер**
(`digest/lint.py`, `lint_digest_html`): полнота номеров дел (с касс.
альтернативами 8Г-…), обёртка `<a><b>`, счётчики `(N)` секций по строкам
с номерами, баланс тегов, футер, лимит длины. Ничего не блокирует — при
аномалии шлёт сервисный 🩺-алерт «Дайджест-линтер» в Telegram (по образцу
детектора здоровья парсеров). Выключатель: `DIGEST_LINT=0`.

Провайдер LLM выбирается переменной `LLM_PROVIDER`
([строка 400](../../scripts/court_monitor/config.py#L400)): `claude` по
умолчанию, `gigachat` или `openrouter`.
Основной мониторинг работает на Claude; GigaChat и OpenRouter доступны из
тестового workflow `test_digest.yml` (inputs `llm_provider` + `llm_model`).
`OPENROUTER_MODEL` принимает буквальный id модели либо место в рейтинге
бесплатных моделей («модель дня (топ-1)», «топ-3»; пусто = топ-1) — тогда
конкретный id резолвится на прогоне с
`https://shir-man.com/api/free-llm/top-models` (`models[N-1].id`,
`_resolve_openrouter_model` с мемоизацией на прогон), при недоступности —
fallback-маршрут `openrouter/free`. Вызовы OpenRouter OpenAI-совместимы
(`_call_openrouter_chat`: Bearer `OPENROUTER_API_KEY`, штатная проверка TLS —
в отличие от GigaChat без `verify=False`).

**Модель Claude:** по умолчанию боевой эталон `claude-haiku-4-5-20251001`;
тестовый workflow может выбрать Sonnet 5 / Opus 4.8 (env `CLAUDE_MODEL`,
алиасы резолвит `config.resolve_claude_model`) и уровень усилий (env
`CLAUDE_EFFORT` → `output_config.effort`, `low`…`max`; пусто = дефолт API
high). ⚠ У моделей нового поколения (Opus 4.7+/Sonnet 5) `temperature`
удалён из API (400 «deprecated for this model») — тело запроса собирает
`llm._claude_payload`: для них adaptive-мышление + effort вместо
температуры, расширенные max_tokens (размышления считаются в лимит вывода)
и таймаут (`_claude_timeout`); haiku-путь неизменен. Кэш пересказов
не-haiku моделей неймспейсится по модели и эффорту (`_act_cache_key`).

## Программный рендер — `generate_template_digest`

[Строка 1737](../../scripts/court_monitor/digest/template.py#L1737). Собирает весь HTML дайджеста
из списков событий (`fi_new_cases`, `changes`, `fi_changes`, `stage_transitions`,
`cass_changes`, `cass_discovered` — см. [05](05-конвейер-обновления.md)). Делит
их по разделам и подсекциям, проставляет нумерацию, формирует «Сводку» и футер.
Telegram-HTML использует только теги `<b>`, `<i>`, `<a href>`.

Если изменений нет — отдаётся «пустой» дайджест через `render_no_changes_digest`
([770](../../scripts/court_monitor/digest/template.py#L770)).

### Что не должно попасть в две секции

Один факт — одна строка. Дело может законно появиться и в 3.2 «Изменения», и
в 3.5 «Вынесенные решения» — но только если у него есть **отдельное побочное**
событие (заседание отложено, назначено новое). Пересказы одного и того же
факта режутся тремя правилами:

| Правило | Где | Что делает |
|---|---|---|
| `types_for_line` | template.py, сборка 3.2 | у дела с `fi_resolved` из строки 3.2 вычёркиваются `fi_resolved` и `fi_status_change`; `fi_act_text_published` всегда уходит в 3.6 и подавляет там же `fi_act_published` |
| `_strip_archive_final_events` | template.py, вход | клерикальное «дело передано в архив» не идёт в дайджест вовсе |
| `_strip_echoed_terminal_events` | template.py, вход | сырая строка события карточки (`fi_final_event`) не печатается у дела, чей исход уже показан (`fi_resolved` или `fi_returned`). Исключение — «Изготовлено мотивированное решение…»: рендер нормализует её в отдельный факт «полный текст не опубликован» |

Оба входных фильтра работают ДО сводки: счётчики сводки считаются по тому же
списку, и правка только в теле дала бы «🏁 1 финальное событие», под которым
в секции пусто.

### Процессуальное завершение 1-й инстанции

Возврат иска, отказ в принятии и передача по подсудности — **не решения по
существу**. Они рендерятся ОДНОЙ строкой в 3.2 и в 3.5 не выводятся
(решение юриста 29.07.2026):

| `details["termination_kind"]` | Строка |
|---|---|
| `returned` | `🔚 иск возвращён: дело не подсудно данному суду (для банка: в пользу банка)` |
| `refusal` | `🔚 отказано в принятии иска: не подлежит рассмотрению в порядке гпк (для банка: …)` |
| `transfer` | `➡️ дело передано по подсудности: в няганский городской суд` |

Хвост «(для банка: …)» несёт знак исхода, который раньше был только в 3.5;
у передачи по подсудности его нет — дело живёт дальше в другом суде. Ключа
`termination_kind` нет в контекстах, сохранённых до 29.07.2026 — рендер
падает на прежнюю форму «🔚 иск возвращён», так что `--replay-last` на старых
снимках не ломается. Сводка считает виды раздельно: «возврат иска» не
покрывает передачу по подсудности.

**Инцидент, из-за которого это сделано (9-336/2026, Урал, 29.07.2026):**
возврат иска пришёл в дайджест дважды — сырым текстом события в 3.2
(«⚖️ Решение вопроса о принятии иска… Возвращение иска… ДЕЛО НЕ ПОДСУДНО…»)
и как «Вынесено решение. Итог: возвращено» в 3.5. Причина — в
[05-конвейер-обновления.md](05-конвейер-обновления.md), шаг 4.

## Пересказ судебного акта — `summarize_act_motivation`

[Строка 871](../../scripts/court_monitor/digest/llm.py#L871). Единственное место, где
LLM реально «думает». Алгоритм:

1. Берётся мотивировочная часть акта (`extract_motive_part`,
   [77](../../scripts/court_monitor/textutil.py#L77)). Слишком короткий текст
   (<100 символов) не пересказывается.
2. Считается ключ кэша (`_act_cache_key`): для Claude —
   `sha1(act_text + "|v3-detailed")[:16]`, для gigachat/openrouter в ключ
   дополнительно входит `провайдер:модель` — тестовый прогон другого
   провайдера не подхватит пересказ Claude и не перезапишет боевой кэш.
   Маркер стиля («v3-detailed» с 14.07.2026 — пересказ в 2–3 предложения
   до ~450 символов) бампается ТОЛЬКО при смене стиля результата: это
   намеренная инвалидация кэша; правки надёжности промпта ключ не трогают.
   Если пересказ уже в кэше `.act_summaries.json` — возвращается он
   (повторно LLM не оплачивается, кэш переживает `--replay-last`).
3. Иначе строится промпт (`_build_act_summary_prompt`,
   [517](../../scripts/court_monitor/digest/llm.py#L517)) — один и тот же для
   всех трёх провайдеров (единственное user-сообщение: роль, задача,
   позитивные правила формулировок, ХОРОШО/ПЛОХО-примеры, текст акта и
   якорь «Ответ (2-3 предложения):» в конце) — и вызывается
   `_call_claude_simple` ([635](../../scripts/court_monitor/digest/llm.py#L635)),
   `_call_gigachat_simple` ([677](../../scripts/court_monitor/digest/llm.py#L677)) или
   `_call_openrouter_simple` (по `LLM_PROVIDER`). max_tokens: 700 у
   Claude/GigaChat, 4096 у OpenRouter — free reasoning-модели (DeepSeek R1,
   Nemotron и т.п.) тратят бюджет на размышления прямо в content и с
   маленьким лимитом обрезаются посреди `<think>`, не дойдя до ответа
   (наблюдалось на nemotron-3-super при 1200). Для openrouter при пустом
   или отбракованном ответе — ретраи с нарастающей паузой
   (`_openrouter_summary_attempts`: до `OPENROUTER_SUMMARY_RETRIES`
   попыток на основной модели, пауза `attempt × OPENROUTER_SUMMARY_RETRY_DELAY`
   — перегруженный free-пул отдаёт 429 мгновенно, и немедленный повтор
   упирался в ту же стену: инцидент 17.07.2026, два акта ушли в дайджест
   сырой мотивировкой), затем фолбэк-модель `OPENROUTER_FALLBACK_MODEL`
   (openrouter/free, сам подбирает живую бесплатную модель) с
   `OPENROUTER_SUMMARY_FALLBACK_RETRIES` попытками; спасение фолбэком
   считается в `METRICS["llm_summary_fallback_saved"]` и логируется INFO.
   Финальный сбой инкрементит `METRICS["llm_summary_failed"]`; пустой
   ответ и отбраковка чисткой логируются WARNING'ом с головой сырого
   ответа, счётчики видны в сводке прогона («LLM-пересказы актов: …,
   спасено фолбэком N, сбоев N (откат на excerpt)»).
4. Ответ чистится (`_clean_summary`) и кладётся в кэш с моделью/датой.
   Чистка рассчитана на free-модели OpenRouter: вырезаются reasoning-блоки
   `<think>…</think>` (в т.ч. незакрытые и осиротевшие), code-fence,
   Markdown-разметка, преамбулы («Вот пересказ:», «Ответ:», «Кратко:» и
   т.п.), переносы строк склеиваются; гарды — нерусский ответ считается
   мусором, неукротимо длинный режется по границе предложения в пределах
   600 символов.
5. При любой ошибке/пустом ответе (в т.ч. после гардов чистки) → `None`,
   и вызывающий код откатывается на сырой excerpt мотивировки
   (`_render_act_summary_or_excerpt`,
   [716](../../scripts/court_monitor/digest/template.py#L716)).

Кэш пересказов: `_load_act_summaries` ([60](../../scripts/court_monitor/storage.py#L60))
и `_save_act_summaries` ([73](../../scripts/court_monitor/storage.py#L73)),
путь — `ACT_SUMMARIES_PATH` (`.act_summaries.json`). Это отдельный кэш от
`.digested_acts` (тот хранит номера дел, чтобы не пересказывать акт дважды).

Пересказ работает для актов **всех трёх инстанций** — `case_meta.stage`
различает 1-ю инст., апелляцию и кассацию (в гибридном пути кассационные
определения идут через тот же `act_summarizer`, в full-LLM — мотивировка
попадает в промпт как «МОТИВИРОВОЧНАЯ ЧАСТЬ ОПРЕДЕЛЕНИЯ»). Для кассации есть
свой дедуп-слой `data/.cassation_acts`: ключи «8Г-номер|дата акта» гасят
повторный `new_act`, когда `act_published` «мигает» из-за сбойного парса
карточки 7kas (см. [05](05-конвейер-обновления.md), шаг 4c).

Точки пересказа в шаблоне (все — через `_act_summary_or_excerpt_with_kind`;
`kind=="summary"` печатается с маркером «**Почему:**», на котором держится
`attach_act_analyses`): 3.6 «Опубликованные тексты решений» (1-я инст.),
5.5 «Опубликованные тексты актов» (апелляция), «Касс. события» (`new_act`),
«Новые касс. дела» (discovery) и — с 13.08.2026 — секция «🏦 ИСКИ БАНКА»
(см. ниже). В discovery-пути текст берётся из details discovery-change'а
(он уже обрезан `extract_motive_part(...,1800)` и загейчен `.cassation_acts`
в linking.py); прямое чтение `cassation.act_text` из cases.json — только
фолбэк для legacy-контекстов replay, с той же обрезкой. До 13.08.2026 этот
путь отправлял в LLM полный акт (до ~10 КБ) и обходил дедуп.

## Полировщик (опционально) — `polish_digest_html`

[Строка 1124](../../scripts/court_monitor/digest/llm.py#L1124). При `DIGEST_POLISH=1`
черновой HTML отправляется в LLM с системным промптом
`_DIGEST_POLISH_SYSTEM_PROMPT` ([973](../../scripts/court_monitor/digest/llm.py#L973)) для
косметики (капитализация, жирные даты, склонения, сокращение длинных категорий).
Результат проходит `_validate_polished_html`
([1080](../../scripts/court_monitor/digest/llm.py#L1080)): проверяется контракт
`<a><b>НОМЕР</b></a>`, наличие `DASHBOARD_URL`, длина. **Если валидация не прошла
— откат к черновику.** Принцип: полировщик не может сделать хуже.

## Слой пост-обработки HTML

После рендера (и до отправки) HTML проходит цепочку детерминированных
«санитайзеров» и валидаторов ([строки 7716–9054](../../scripts/court_monitor/digest/core.py#L1250)).
Они гарантируют корректность вёрстки и согласованность счётчиков:

| Функция | Что гарантирует |
|---------|-----------------|
| `_wrap_all_bare_case_numbers` ([81](../../scripts/court_monitor/digest/postprocess.py#L81)) | Все «голые» номера дел обёрнуты в ссылки на карточки. |
| `_ensure_appeal_new_case_full_layout` ([158](../../scripts/court_monitor/digest/postprocess.py#L158)) | Полная вёрстка блока нового апел. дела. |
| `_validate_digest_new_sections` ([266](../../scripts/court_monitor/digest/postprocess.py#L266)) / `_drop_hallucinated_from_section` ([332](../../scripts/court_monitor/digest/postprocess.py#L332)) | Отсев галлюцинаций (только для full-LLM-режима). |
| `_renumber_section_headers` ([455](../../scripts/court_monitor/digest/postprocess.py#L455)) / `_drop_zero_count_sections` ([1149](../../scripts/court_monitor/digest/postprocess.py#L1149)) | Перенумерация разделов, выкидывание пустых. |
| `summarize_digest_counters` ([785](../../scripts/court_monitor/digest/postprocess.py#L785)) / `_compute_summary_lines` ([815](../../scripts/court_monitor/digest/postprocess.py#L815)) / `_replace_summary_block` ([1009](../../scripts/court_monitor/digest/postprocess.py#L1009)) | Блок «📋 Сводка» считается по фактическому HTML — те же цифры, что и в push. |
| `_normalize_section_spacing` ([606](../../scripts/court_monitor/digest/postprocess.py#L606)) | Отступы: строки одного дела подряд, пустая строка только между разными делами. |
| `_shorten_categories_in_html` ([1123](../../scripts/court_monitor/digest/postprocess.py#L1123)) | Сокращение длинных категорий. |
| `_purge_3_6_without_act_text` ([1209](../../scripts/court_monitor/digest/postprocess.py#L1209)) | Чистка раздела без текста акта. |
| `_ensure_footer` ([561](../../scripts/court_monitor/digest/postprocess.py#L561)) | Футер со ссылкой на дашборд. |
| `_close_open_tags` ([1343](../../scripts/court_monitor/digest/postprocess.py#L1343)) / `_strip_orphan_close_tags` ([1359](../../scripts/court_monitor/digest/postprocess.py#L1359)) / `truncate_html_message` ([1386](../../scripts/court_monitor/digest/postprocess.py#L1386)) | Закрытие тегов и безопасная обрезка под лимит Telegram. |

## Структура дайджеста

- **Шапка:** `📊 Дайджест судебных дел | Суды ХМАО-Югры | ДД.ММ.ГГГГ`.
- **📋 Сводка:** счётчики по инстанциям и типам событий (из фактического HTML).
- **Разделы по типам событий:** новые дела (1-я инст. / апелляция / кассация),
  изменения, переходы стадий, опубликованные акты с пересказом. Нумеруются и
  чистятся от пустых. С 13.08.2026 (разбор основного прогона юристом):
  «Вынесенные акты» апелляции печатают нормализованный «Итог: … Для банка: …»
  вместо сырого поля «Результат» (replay-фолбэк), тексты решений 3.6 несут
  дату решения, новые апелляции — номер дела 1-й инст. в строке 1 и пометку
  «🔁 повторное рассмотрение после кассации» у второго круга (матч по свежему
  cases.json), кассация получила события заседаний (`cass_hearing_scheduled`)
  и «без движения» (`cass_suspended`) + хвост «→ в суд … инстанции» у
  remanded, 3.2 — заседания по решённым делам (`fi_post_decision_hearing`
  теперь оба трека), вручение копии заочного и тип заседания скобками.
- **Секция «🏦 ИСКИ БАНКА (N)»** — последней: компакт «одна строка на дело»,
  записи `fi_changes` с маркером `track == "plaintiff_light"` отщепляются от
  основного списка. Фразы — `_bank_event_phrases`
  (template.py), группировка «по важности» — `_BANK_GROUP_ORDER`. С 13.08.2026
  строки несут даты/время (срок возражений, отмена заочного, жалобы,
  мотивировка, заседания с временем; replay-safe фолбэк на голую подпись) +
  четыре события: `fi_legal_force_reached`, `fi_writ_overdue` (календарный
  проход, см. [05](05-конвейер-обновления.md)), `fi_post_decision_hearing`
  (заседание по решённому делу), `fi_default_copy_served` (вручение копии
  заочного). Сводка пишет «N дел с событиями» (счёт по делам, не событиям).
  **С 14.08.2026 — свёртка массовых заведений** (`split_bank_intake_fold`,
  порог `BANK_INTAKE_DIGEST_FOLD`=25): больше порога дел, у которых
  `fi_bank_claim_registered` — единственный тип, печатаются одной строкой
  «🆕 заведено N новых исков банка в M судах» (разгон Урала: 116 одинаковых
  строк раздули HTML до 60 КБ). Счётчик заголовка = число ПОДРОБНЫХ дел, при
  нуле подробных заголовок без `(N)`; сводка получает отдельную часть
  «🆕 N новых исков банка заведено». Тот же хелпер гейтит линтер
  (`_expected_number_alternatives`) и валидатор полировщика
  (`llm._collect_case_numbers`) — иначе они объявят свёрнутые номера
  потерянными.
  С 13.08.2026 у `fi_act_text_published` с исходом **против банка**
  (`bank_act_why_eligible`: `bank_outcome ∉ {"", "в пользу банка"}` — отказ,
  частичное, прекращение с учётом роли) второй строкой печатается LLM-пересказ
  «Почему» — без номера дела (линтер считает дела по строкам с номерами) и
  строго без excerpt-фолбэка (Telegram-бюджет: при отказе LLM остаётся
  прежняя одна строка). Полные удовлетворения не пересказываются —
  мотивировка шаблонна (решение юриста).
- **Футер:** ссылка на дашборд (+ приписка «🏦 иски банка: N в производстве»).

Лимит Telegram — 4096 символов на сообщение; длинный дайджест автоматически
режется на части (`split_message`, см. [07](07-доставка-и-уведомления.md)).
Целевой объём задаётся `DIGEST_CHAR_LIMIT` ([521](../../scripts/court_monitor/config.py#L521)).

## Разбор акта в карточке (`act_analysis`)

`attach_act_analyses` ([180](../../scripts/court_monitor/digest/core.py#L180)) после рассылки
сохраняет LLM-разбор опубликованных актов в поле `act_analysis` соответствующих
дел в `cases.json` — чтобы юрист видел его в drawer'е дашборда дольше одного дня.
Обновляются только дела с новым актом в этом прогоне.

С 13.08.2026 — второй вызов для банк-дел (runs.py, после основного): к
моменту attach они уже разложены из `cases` в `bank_active` (split_bank_track,
фаза 7c), и первый вызов их не находил. Гейт — тот же `bank_act_why_eligible`,
что у рендера банк-секции; цели фильтруются по `court_domain` из details
(номера банк-дел не уникальны между судами); `require_explained=True` — без
«Почему»-абзаца сразу raw_act-фолбэк (банк-дело печатается одной строкой с
номером, и обычный фолбэк «любой абзац с номером» выдал бы её за анализ).
При обновлениях `cases_bank.json` пересохраняется повторно.

## Контекст и replay

Перед отправкой `save_digest_context` ([54](../../scripts/court_monitor/digest/core.py#L54))
сохраняет снимок всех входных списков в `last_digest_context.json`. Это позволяет
режиму `--replay-last` переиграть дайджест с обновлённым промптом, не запрашивая
суды заново (LLM-пересказы при этом берутся из кэша — повторно не оплачиваются).
Готовый HTML кладётся в `last_digest.json` (`save_last_digest`,
[99](../../scripts/court_monitor/digest/core.py#L99)) для блока «Последний дайджест» на фронте.

> ⚠️ Промпты (`GIGACHAT_SYSTEM_PROMPT` [76](../../scripts/court_monitor/digest/llm.py#L76),
> `_build_act_summary_prompt`, `_DIGEST_POLISH_SYSTEM_PROMPT`) долго настраивались
> вручную. Менять их структуру — только осознанно и с предупреждением; см.
> «Чего НЕ делать» в [`CLAUDE.md`](../../CLAUDE.md).

Куда уходит готовый дайджест и как он персонализируется — в
[07. Доставка и уведомления](07-доставка-и-уведомления.md).
