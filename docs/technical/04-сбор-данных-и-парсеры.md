# 04. Сбор данных и парсеры

> **Состав публикации документации.** Автопоиск открытых президиумов, расширенный импорт КСОЮ и новые проверки связок ниже описывают подготовленную локальную реализацию. Эти изменения кода не входят
> в исходный коммит эталона `1a943a7f` и этим выпуском документации не публикуются.
> [Границы снимка и выпуска](../Сверка_документации_2026-09-13.md).

## Что это и зачем

Это «глаза» системы — слой, который ходит на публичные сайты судов
(ГАС «Правосудие», `*.sudrf.ru`), скачивает страницы и превращает сырой HTML в
структурированные данные о делах. Никакого API у судов нет: данные приходится
выскребать из HTML-таблиц («скрейпинг»). Из-за этого парсеры — самая хрупкая
часть системы: меняется вёрстка сайта → ломается парсинг. Поэтому здесь много
защитных проверок и эмпирически подобранных констант, которые **нельзя менять
без проверки на живом сайте**.

Юристу важно понимать одно: всё, что показывает дашборд, добыто отсюда. Если
дело «пропало» или данные неполные — почти всегда причина в том, что суд
изменил карточку или временно недоступен.

## Как скрипт ходит на сайты судов

| Функция | Строка | Что делает |
|---------|--------|-----------|
| `polite_delay()` | [netutil.py](../../scripts/court_monitor/netutil.py) | Случайная пауза `REQUEST_DELAY` = 2–3 сек между запросами, чтобы не нагружать суд и не попасть под блокировку. |
| `fetch_page(url)` | [netutil.py](../../scripts/court_monitor/netutil.py) | Скачивает страницу. Боевой дефолт — **одна попытка** (`FETCH_MAX_RETRIES=1`). Mac-резерв поднимает только потолок до 3, но `should_retry_fetch` реально повторяет лишь быстрый `connection_reset`/ошибку ответа/5xx, завершившиеся не дольше `FETCH_RETRY_FAST_MAX_SECONDS`; timeout, 4xx, CAPTCHA и HTTP-200-заглушки не повторяются. **Декодирует из `windows-1251`**. Возвращает `""` при провале. |
| `parse_date(s)` | [textutil.py](../../scripts/court_monitor/textutil.py) | Парсит судебный формат `ДД.ММ.ГГГГ` в `datetime`. |

HTTP-сессия (`requests.Session`) с браузерным `User-Agent` создаётся один раз
([`session`, netutil.py](../../scripts/court_monitor/netutil.py)). Счётчики запросов копятся в
`METRICS` для итоговой сводки прогона.

### Точные классы отказов и пер-суд circuit breaker

Карточки дел и тексты актов качаются через `fetch_card_checked`: поверх
`fetch_page` он распознаёт HTTP-200 ответы, которые карточками не являются, и
возвращает `""`, чтобы ими не бумпался `last_checked_at`. Классификация живёт
в одном месте: транспорт — `transport_fail_kind`, HTML-семантика —
`classify_outage_page` / `classify_non_card_page`; breaker получает уже
готовый `kind` и не анализирует исключение или HTML второй раз.

| Операционный класс | Точные `kind` | Порог полного прогона | Cooldown |
|---|---|---:|---:|
| Быстрый временный сбой | `connection_reset`, `response_error`, HTTP 500/502/503/504 | 3 | 60 с |
| Медленная недоступность | `read_timeout`, `connect_timeout`, прочие connection/DNS/TLS/proxy | 2 | 300 с |
| Портал лежит/вернул служебную страницу | `portal_placeholder`, `outage_search`, `non_card_page` | 2; известная заглушка поиска пре-открывает сразу | 180 с |
| Блок доступа | `waf_block`, `waf_search`, HTTP 403, `captcha_card` | 2; WAF поиска пре-открывает сразу | 600 с |
| Качество парсера, не сеть | `empty_shell`, `empty_search`, `unparsed_card`, `degraded_card`, `captcha_search` | breaker не открывает | — |

Разные семейства в полном прогоне не складываются: один reset и одна CAPTCHA
не превращаются в «два timeout подряд» и не получают чужой cooldown. Успех
сбрасывает серию. Ретраи одного `fetch_page` считаются одной логической
неудачей breaker; общий `FETCH_MAX_RETRIES` эта политика не повышает.

Поверх этого — **предохранитель** (аутейдж Сургутского городского 29.07.2026:
суд отдавал заглушку на каждой карточке, а прогон впустую тратил 2–3 с паузы
и HTTP-запрос на каждое его дело):

- **Два профиля.** Полный утренний обход явно запускается с
  `CARD_BREAKER_MODE=time` в GitHub workflow и Mac `parse_and_push.sh`.
  Операторские batch-импорты явно остаются на `count` с прежними значениями
  `CARD_BREAKER_THRESHOLD=5` / `CARD_BREAKER_PROBE_EVERY=3`: короткий дамп
  закончится раньше временного cooldown, и смена профиля лишила бы его проб.
- **Канарейка из фазы поиска**: страница поиска суда грузится раньше обхода
  карточек, и точная заглушка/WAF на ней пре-открывает предохранитель — ни
  одна карточка не тратится. CAPTCHA поиска breaker НЕ открывает: у
  `search_gated`-судов поиск закрыт штатно, а карточки доступны.
- **Time-based half-open и отложенная очередь.** Открытый хост не блокирует
  весь цикл: его карточка кладётся в `DeferredCardQueue`, а парсер продолжает
  другие суды. После `next_probe_at` допускается ровно одна проба; успех
  закрывает breaker и возвращает весь хвост хоста в ту же очередь. Первая
  неудачная half-open проба исчерпывает бюджет хоста в ЭТОЙ фазе: новый
  cooldown сохраняется в telemetry, но второй круг проб в той же очереди не
  запускается, даже если работа по другим судам длилась дольше cooldown. В
  конце стадии выполняется только no-sleep sweep: если срок ещё не наступил
  или проба фазы уже провалилась, процесс не ждёт искусственно — остаток
  честно остаётся недочитанным до следующего слота. Новая фаза получает свой
  отдельный бюджет одной неудачной пробы.
- Состояние (`config.CARD_BREAKER`) живёт **один прогон** — сброс в
  `_metrics_reset`, ничего не залипает между слотами. Бизнес-гейты
  (smart-skip, denominator, bank-report) выполняются до очереди один раз и не
  задваиваются при возврате карточки после half-open.
- Наблюдаемость разделяет три числа: сколько раз гейт сработал
  (`cards_breaker_skipped`), сколько отложенных карточек дочитано
  (`cards_breaker_recovered`) и сколько **так и осталось без HTTP** в основных
  планах (`cards_breaker_unrequested` / совместимый `cards_unreachable`). В
  атомарный `parse_telemetry.json` и `parse_health.last_run.breaker` едут
  точный `kind`, состояние хоста, cooldown, пробы и остаток очереди.

Если сайт действительно лежит, полный прогон больше не тратит timeout на
каждую его карточку. Для медленного хоста до открытия — максимум две
логические неудачи (при текущем `FETCH_TIMEOUT_READ=65`: примерно 130 секунд),
затем не более одной 65-секундной half-open пробы **за фазу**. Провал не
разрешает вторую пробу в этой фазе, поэтому множество лежащих хостов не может
само пережидать cooldown по кругу и удерживать процесс до восстановления.
Сам cooldown не добавляет времени прогона: `sleep` ради него нет.
Заглушка/WAF с HTTP 200 обычно распознаётся быстро и открывает breaker ещё
дешевле.

## `CourtConfig` — описание одного суда

[base.py](../../scripts/court_monitor/regions/base.py). Dataclass, описывающий, как
обращаться к конкретному суду. Поля: `name`, `domain`, `delo_id`, `court_type`,
`srv_num`. Ключевое — `delo_id`, оно определяет тип производства и, как следствие,
имена таблиц и полей в URL:

| `delo_id` | Инстанция | `delo_table` | `name_field` | `&new=` |
|-----------|-----------|--------------|--------------|---------|
| `5` | Апелляция (Суд ХМАО-Югры) | `g2_case` | `G2_PARTS__NAMESS` | `5` |
| `1540005` | 1-я инстанция (гражд.) | `g1_case` | `G1_PARTS__NAMESS` | `0` |
| `2800001` | Кассация 7-го КСОЮ (гражд.) | `g33_case` | `G33_PARTS__NAMESS` | `2800001` |

Методы `search_url()` ([base.py](../../scripts/court_monitor/regions/base.py)) и `card_url()`
([base.py](../../scripts/court_monitor/regions/base.py)) собирают URL поиска и карточки. Поиск
ведётся по стороне дела — имя «Сбербанк» передаётся **закодированным в
windows-1251** (`SBER_NAME_WIN1251`), иначе сайт не найдёт совпадений.

Реестры активной территории: `APPEAL_COURTS` (и legacy-алиас `APPEAL_COURT`),
`FIRST_INSTANCE_COURTS`, `CASSATION_COURT` в
[`courts.py`](../../scripts/court_monitor/courts.py); президиумы — отдельный
кортеж `RegionConfig.presidium_courts`. Число судов зависит от региона.

> ⚠️ **Параметры 7kas подобраны эмпирически.** `delo_id=2800001`,
> `delo_table=g33_case`, `name_field=G33_PARTS__NAMESS`, `new=2800001` — это
> отдельная ветка для КСОЮ. Любое «логичное» значение (`new=0`, `new=5`,
> `ka1_case`) приводит к молчаливому «Данных по запросу не обнаружено». Менять
> только после ручной проверки на 7kas.

## `TableExtractor` — разбор HTML-таблиц

[tables.py](../../scripts/court_monitor/parsing/tables.py). Подкласс стандартного
`html.parser.HTMLParser`. Извлекает **все** `<table>` со страницы как списки
строк (списки ячеек). Особенности:

- **Поддержка вложенных таблиц** через стеки. Таблица регистрируется в момент
  открытия тега — это сохраняет порядок документа (внешняя раньше внутренней).
  Критично для вкладки «Обжалование»: внешняя таблица «ЖАЛОБА № N» (с видом
  жалобы — апелляционная/кассационная) должна попасть в список раньше вложенной
  «ДВИЖЕНИЕ ЖАЛОБЫ». Плоская версия теряла факт подачи жалобы.
- **Ссылки внутри ячеек** сохраняются через маркер `\x00HREF:`; достаются
  хелперами `cell_text()` ([tables.py](../../scripts/court_monitor/parsing/tables.py)) и
  `cell_href()` ([tables.py](../../scripts/court_monitor/parsing/tables.py)).

`extract_tables(html)` ([tables.py](../../scripts/court_monitor/parsing/tables.py)) — фасад.

## Парсеры поисковой выдачи

Поиск возвращает список дел; детали потом дочитываются из карточек.

### Апелляция — `parse_search_page`
[search.py](../../scripts/court_monitor/parsing/search.py). Таблица результатов —
6-я на странице (индекс 5). Из объединённой ячейки «категория/стороны/суд»
данные выбирает `_parse_combined_cell` ([search.py](../../scripts/court_monitor/parsing/search.py))
регулярками. Роль банка определяется по тому, в какой стороне найден один из
`SBER_PATTERNS`. **Дела, где «Сбербанк» — только дочка (страхование/НПФ/лизинг),
отсеиваются** (см. ниже).

### 1-я инстанция — `parse_first_instance_search`
[search.py](../../scripts/court_monitor/parsing/search.py). Таблица результатов ищется
не по индексу, а по содержимому заголовка (`_find_results_table`,
[search.py](../../scripts/court_monitor/parsing/search.py)) — у судов 1-й инстанции она на
другой позиции.

Флаг `keep_all_roles` управляет фильтром роли банка. По умолчанию функция
отдаёт только «банк-ответчик» (историческое поведение, на нём стоят импортёр
дампов и его тесты). Боевой прогон с 31.07.2026 зовёт её с
`keep_all_roles=True` и делит строки по `bank_role` сам: ответчик-дела — в
основной трек, истцовые — в авто-подхват трека «Иски банка» (блок 3b фазы 3).
Счётчик здоровья `stats["sber_rows"]` считается ДО фильтра роли и от режима не
зависит.

### Кассация — `parse_cassation_search_page`
[cassation.py](../../scripts/court_monitor/parsing/cassation.py). Разбирает выдачу
КСОЮ и президиумов. Для каждой строки пытается определить суд первой инстанции
через `match_hmao_first_instance` ([courts.py](../../scripts/court_monitor/courts.py)):
несмотря на старое имя, это обёртка матчера активной территории. Он нормализует
ё→е и требует региональный маркер, чтобы не смешать одноимённые районные суды.
Сам парсер возвращает все строки; основной поиск КСОЮ отсеивает строки без
`fi_court_config` до чтения карточек. Президиум использует отдельный маршрут,
поскольку мировые участки не входят в реестр районных судов. Автоматическая
пагинация не обходится — только первая страница.

## Фильтр «настоящего» Сбербанка

Поиск по слову «Сбербанк» цепляет и дочерние структуры. Их нужно отсеять, иначе
в мониторинг попадут чужие дела.

- `_SBER_SUBSIDIARY_PATTERNS` ([search.py](../../scripts/court_monitor/parsing/search.py)) —
  регулярки дочек: «Сбербанк страхование [жизни]», «НПФ Сбербанк», «Сбербанк
  Управление Активами», «Сбербанк Лизинг» и т.п.
- `is_subsidiary_only_case(plaintiff, defendant)`
  ([search.py](../../scripts/court_monitor/parsing/search.py)) — `True`, если «сбербанк» в
  сторонах встречается **только** в названии дочки. Такие дела пропускаются.
- `_is_real_sberbank(name)` ([search.py](../../scripts/court_monitor/parsing/search.py)) и
  `determine_bank_role_from_participants(participants)`
  ([search.py](../../scripts/court_monitor/parsing/search.py)) — пересчёт роли банка по списку
  участников из карточки (приоритет: Ответчик > Истец > Третье лицо). Если
  ПАО Сбербанка нет вовсе — для кассации это сигнал отбросить дело, для 1-й
  инстанции — поставить нейтральное «Третье лицо».

## Парсеры карточек

### Карточка 1-й инстанции / апелляции — `parse_case_card`
[cards.py](../../scripts/court_monitor/parsing/cards.py). Самая большая парсер-функция.
Из карточки извлекает: последнее событие и дату (вкладка «Движение дела»),
результат, статус, наличие и текст судебного акта, судью/докладчика, **УИД** и
«Номер дела в первой инстанции» (для связки инстанций), список участников
(для пересчёта роли). Отдельно — флаги движения по инстанциям из вкладки
«Обжалование решений»: подана апелляционная/кассационная жалоба, направлено в
вышестоящий суд, с массивами событий и датами. Эти флаги (`_fi_appeal_filed`,
`_fi_cassation_filed`, `_fi_sent_to_*` …) и питают машину состояний из
[03. Жизненный цикл](03-жизненный-цикл-дела.md).

`_warn_if_card_degraded` ([cards.py](../../scripts/court_monitor/parsing/cards.py)) ловит
ситуацию «карточка пришла обрезанной» (мало таблиц) — чтобы не затереть хорошие
данные пустыми.

### Карточка кассации — `parse_cassation_card`
[cassation.py](../../scripts/court_monitor/parsing/cassation.py). Общий парсер
карточек КСОЮ и президиумов; параметры URL задаёт `CourtConfig` нужного суда.
Возвращает `None`, если нет блока «Рассмотрение в нижестоящем суде» (карточка
не та). Достаёт УИД, даты, категорию, судью, результат рассмотрения и его
влияние на акт апелляции, данные нижестоящего суда (через
`match_hmao_first_instance`), слушания, кассатора и его статус, участников, а
также **текст определения** из скрытого `<div id="cont_doc1">`.

### Карточка и выдача ПРЕЗИДИУМА облсуда (04.09.2026)

Тот же парсер, что у 7kas (раздел `delo_id=2800001` на домене апел-суда,
номера «4Г-N/YYYY»): `_CASS_INTERNAL_NUM_RE` обобщён до `\d+Г-…`, второй
номер в квадратных скобках («4Г-17/2026 [44Г-2/2026]») → `cassation_number`
(аналог `88-` у КСОЮ; `_bare_case_number` скобки не режет — режет парсер),
«Судья (мировой судья) первой инстанции: ФИО (участок)» в объединённой
ячейке и в карточке → `fi_judge` + `fi_court_long` «Мировой судья (участок)»
+ `fi_magistrate` (`split_magistrate_judge`), номер с заголовка карточки
(`div.casenumber`) → `page_case_number` (при перечитке строки выдачи нет),
`court_domain` — из `court_base_url` (по нему блок находит свой суд).
Роли приказного производства (ВЗЫСКАТЕЛЬ/ДОЛЖНИК) — только здесь
(`determine_bank_role_from_participants(synonyms=True)`), карточки 1-й
инстанции не задеты. Исходы «СУДЕБНЫЙ ПРИКАЗ ОТМЕНЕН» → `cassation_reversed`,
«… с направлением на новое рассмотрение» → `cassation_remanded`.
Фикстуры — реальные страницы Суда ХМАО-Югры (обезличены):
`case_card_presidium.html`, `search_presidium_dump_hmao.html`.

### Автопоиск президиума и ручной импорт кассации

Этот раздел описывает локальную реализацию, не включённую в код данного выпуска.

`scripts/court_monitor/presidium_search.py` (локальная разработка, файл не включён в этот выпуск) собирает
первую страницу открытого президиума и читает карточки кандидатов до общей
`link_cassation_cases`. `enabled=False`, `search_gated` или `search_disabled`
полностью выключает запрос поиска; успешный поиск в тот же день повторно не
выполняется. Эти пропуски не становятся HTTP-ошибкой или пустой выдачей в журнале
здоровья. Капча и заглушка открытого поиска получают собственный диагноз.

Отсечка `PRESIDIUM_SINCE = "01.05.2026"` общая с дампом: более ранние жалобы
не запрашиваются; неизвестная дата сама по себе не отсекает строку. Поиск требует
успешной карточки с ПАО Сбербанк в участниках. Судебный участок не обязан входить
в реестр районных судов. План карточек фиксируется до запросов: непрочитанные
карточки остаются в знаменателе, даже если их отложил предохранитель.

Ручные дампы КСОЮ и президиума обрабатывает `import_cassation_rows` в
[`import_search_dump.py`](../../scripts/import_search_dump.py). Для КСОЮ
проверяется принадлежность суда первой инстанции активной территории — сначала
по строке, затем по прочитанной карточке. Для президиума действует отдельный
маршрут мировых судей и отсечка даты; для КСОЮ такой отсечки нет. В обеих ветках
карточка обязательна, проверка ПАО Сбербанк идёт по её участникам, а неоднозначная
связка выводится как `[NEEDS REVIEW]`. Строки с кассационными номерами, но без
распознаваемых ссылок карточек дают ошибку дампа, а не успешный пустой импорт.

Для сопоставления суда используются подтверждённые `CourtConfig.name_aliases`.
Матчер полного имени требует региональный маркер и единственный подходящий
домен; неоднозначность не разрешается порядком реестра. Стражи:
`test_presidium_search.py`, `test_import_cassation_dump.py`,
`test_appeal_court_identity.py`; структура карточки 6kas сохранена в
`case_card_6kas.html`.

### Текст актов
- `_extract_act_text(html)` ([cards.py](../../scripts/court_monitor/parsing/cards.py)) и
  `fetch_act_text(url)` ([cards.py](../../scripts/court_monitor/parsing/cards.py)) — для
  1-й инстанции/апелляции.
- `_extract_cassation_act_text(html)`
  ([cassation.py](../../scripts/court_monitor/parsing/cassation.py)) — из `cont_doc1` на 7kas.

Текст акта нужен LLM, чтобы пересказать мотивировку
(см. [06. Дайджесты и LLM](06-дайджесты-и-llm.md)).

## Классификаторы исходов

Сырые формулировки судов нормализуются в фиксированные категории —
детерминированно, без LLM, чтобы исход было нельзя «нагаллюцинировать».

| Функция | Строка | Что классифицирует |
|---------|--------|--------------------|
| `classify_verdict(result, last_event)` | [lifecycle.py](../../scripts/court_monitor/lifecycle.py) | Исход апелляции: оставлено / отменено / изменено и т.п. |
| `classify_verdict_fi(result)` | [lifecycle.py](../../scripts/court_monitor/lifecycle.py) | Исход 1-й инстанции (удовлетворено/отказано/…). |
| `classify_cassation_outcome(result_text, result_for_appeal, review_result)` | [cassation.py](../../scripts/court_monitor/parsing/cassation.py) | **Enum исхода кассации** (см. ниже). Управляет переходом `cassation → awaiting_relink`. |
| `cassation_remanded_to(result_for_appeal, act_text)` | [cassation.py](../../scripts/court_monitor/parsing/cassation.py) | Куда направлено при отмене: `first_instance` / `appeal`. |
| `bank_side_outcome_fi(role, verdict)` | [lifecycle.py](../../scripts/court_monitor/lifecycle.py) | Перевод исхода 1-й инст. в «для банка»: выигрыш/проигрыш/частично. |
| `bank_side_outcome(role, appellant, verdict)` | [lifecycle.py](../../scripts/court_monitor/lifecycle.py) | То же для апелляции с учётом, кто апеллянт. |

### Enum исхода кассации

`classify_cassation_outcome` маппит поля карточки в одно из значений (русские
подписи — в `CASSATION_OUTCOME_RU`, [cassation.py](../../scripts/court_monitor/parsing/cassation.py)):

| Enum | Подпись |
|------|---------|
| `cassation_dismissed_no_transfer` | 🚫 Отказ в передаче |
| `cassation_upheld` | Оставлено без изменения |
| `cassation_modified` | Изменено |
| `cassation_reversed` | Отменено |
| `cassation_remanded` | 🔁 Отменено и направлено на новое рассмотрение |
| `cassation_terminated` | 🛑 Прекращено / отозвано / возвращено |
| `cassation_other` | _(пусто — нестандартная формулировка)_ |
| `""` | _(карточка ещё в производстве)_ |

## Что делать при поломке парсера

- Сначала **открыть карточку дела на сайте суда вручную** и сравнить с тем, что
  ожидает парсер (индекс таблицы, заголовки колонок).
- Для 7kas — проверить, что поиск вообще что-то возвращает с текущими
  параметрами (см. предупреждение выше).
- Тесты парсеров — в [`scripts/tests/test_parsing.py`](../../scripts/tests/test_parsing.py)
  с зафиксированными HTML-`fixtures/`. Добавляя обработку нового кейса, кладите
  фикстуру и пишите тест (см. [10. CI/CD и эксплуатация](10-ci-cd-и-эксплуатация.md)).

Как все эти парсеры вызываются в правильном порядке за один прогон — в
[05. Конвейер обновления](05-конвейер-обновления.md).
