# Сверка документации после изменений 11–13 сентября 2026 года

Дата: 13.09.2026. Репозиторий: `dashboard`, HEAD на начало сверки — `1a943a7f`.

Документация приведена к текущим файлам, включая незакоммиченные изменения,
которые уже находились в рабочем каталоге. Сверка продолжает
[аудит 11 сентября](Аудит_инструкций_Astra_2026-09-11.md), объединивший
инструкции в `AGENTS.md`. Исторические отчёты сохраняют свои даты и границы.

## Что актуализировано

| Тема | Что теперь описано | Источники реализации |
|---|---|---|
| Входные инструкции | `AGENTS.md` — единая карта; `CLAUDE.md` ссылается на неё. Учтены Башкортостан, очередь, шлюз, время суда и отдельная доставка. | [AGENTS.md](../AGENTS.md), [CLAUDE.md](../CLAUDE.md) |
| Обзор проекта | Python-пакет вместо монолита, VPS как основной исполнитель, Mac как резерв, отдельные региональные картотеки; убрана прежняя схема Worker-cron как текущий запуск. | [CLI-фасад](../scripts/update_cases.py), [пакет](../scripts/court_monitor/__init__.py), [Worker-конфигурация](../cloudflare-worker/wrangler.toml) |
| VPS и доставка | Независимые службы парсинга/импорта, отдельный таймер доставки, общий lock, завершённый день без новых событий, повторы незакрытых выпусков. | [службы systemd](../ops/vps-run/systemd/), [доставка](../ops/mac-local-run/delivery_all.sh), [готовность](../ops/mac-local-run/cloud_run_ok.py), [транзакция](../ops/mac-local-run/parse_and_push.sh) |
| Полная очередь | Форма освобождается после приёма, задания отслеживаются по UUID; `queue` содержит полную очередь за срок хранения, `items` — последние 50 записей истории. | [Worker](../cloudflare-worker/worker.js), [админка](../cloudflare-worker/admin_page.js), [отбор заданий](../ops/mac-local-run/import_queue.jq) |
| Шлюз импортов | Общий модуль Worker и отдельный `IMPORT_GATEWAY_ORIGIN` территории, проверка загружаемого содержимого до записи в KV. | `cloudflare-worker/import_gateway.js` (локальная разработка), [Worker](../cloudflare-worker/worker.js) |
| Регионы и инстанции | Открытые и закрытые президиумы, ручной импорт КСОЮ, проверка территории и неоднозначных связок, отдельные данные и конфиги Башкортостана. | [реестры](../scripts/court_monitor/regions/), `scripts/court_monitor/presidium_search.py` (локальная разработка), [импорт](../scripts/import_search_dump.py), [связки](../scripts/court_monitor/linking.py) |
| Время и свежесть | Пояс суда отличается от пояса территории; часы заседания сохраняются, другая зона подписывается, timed-события ICS переводятся в UTC. `updated_at` картотеки и отчёта здоровья содержит смещение. | [конфигурация](../scripts/court_monitor/regions/base.py), [UI](../app.js), [календарь](../cloudflare-worker/worker.js), [шаблон](../scripts/court_monitor/digest/template.py), [хранение](../scripts/court_monitor/storage.py), [здоровье](../scripts/court_monitor/health.py) |
| LLM и публикация | Различаются дефолт Python и workflow, кэш моделей, Telegram и отдельный Web Push, попытка подтверждения Pages и её ограничения. | [config.py](../scripts/court_monitor/config.py), [LLM](../scripts/court_monitor/digest/llm.py), [replay](../.github/workflows/replay_on_push.yml) |

Коммит `141460a5` от 12 сентября содержит новую очередь импортов;
`1a943a7f` от 13 сентября — отдельную доставку и независимые импорты VPS.
Поддержка Башкортостана, изменения связок, времени и часть интерфейса на
начало сверки также присутствовали как локальные правки. Документирование
реализации не означает, что весь этот состав уже опубликован.

## Где читать подробности

- [README](../README.md) и [обзор архитектуры](technical/01-обзор-и-архитектура.md).
- [Модель данных](technical/02-модель-данных.md),
  [жизненный цикл](technical/03-жизненный-цикл-дела.md),
  [парсеры](technical/04-сбор-данных-и-парсеры.md),
  [конвейер](technical/05-конвейер-обновления.md).
- [Дайджест и LLM](technical/06-дайджесты-и-llm.md),
  [доставка](technical/07-доставка-и-уведомления.md),
  [интерфейс](technical/08-фронтенд.md), [Worker](technical/09-cloudflare-worker.md).
- [Эксплуатация](technical/10-ci-cd-и-эксплуатация.md),
  [VPS](../ops/vps-run/README.md), [Mac-резерв](../ops/mac-local-run/README.md).
- [Региональная изоляция](Тиражирование_регионы.md),
  [инструкция Башкортостана](regions/Башкортостан_оператор.md).

## Проверки локальной сверки 13 сентября

Изменены 20 существующих Markdown-документов и добавлен этот отчёт — всего 21.

- Проверены 477 локальных ссылок: цели существуют; среди них 10 ссылок на
  заголовки Markdown и 193 ссылки формата `#L` на строки кода. Ошибок нет.
- Отдельно разобраны 26 устаревших ссылок, которые не мог исправить обычный
  проход переанкеровщика: числовые заголовки этапов, диапазоны и поля JSON
  вместо имён функций. Они заменены точными ссылками на определения либо
  устойчивыми ссылками на модуль и именованный этап. Тестовые исключения
  не расширялись.
- `python3 scripts/refresh_doc_anchors.py`: **0** ссылок к обновлению,
  **0** нераспознанных символов.
- `python3 -m pytest scripts/tests/test_doc_anchors.py -q`: **3 passed**,
  без пропусков.
- `git diff --check`: без ошибок.
- Сравнение SHA-256 с состоянием на начало задачи: недокументационные файлы
  не изменены, существующие файлы не удалены. Прежние локальные правки кода
  и данных сохранены.

Полный pytest продукта, CI, production API, живые таймеры VPS, сеть операторов
и фактическая доставка этой сверкой не проверялись. Коммит, push,
синхронизация региональных копий и деплой на этом этапе не выполнялись;
результат оставался в рабочем каталоге.


## Документационный выпуск 14 сентября

По отдельному поручению подготовлены commit, push и синхронизация Урала и
Башкортостана. Состав — 28 документационных файлов: обновлённые инструкции,
технические главы и рантбуки, этот отчёт, архив инструкций 11 сентября,
датированные региональные документы и справочник судов в JSON/CSV.
Код, данные картотек, настройки служб и секреты в этот выпуск не входят.

База публикуемого эталона — `1a943a7f`: полная очередь импортов и отдельная
доставка уже есть в коде. Следующие описания относятся к подготовленному
локальному пакету, который этим коммитом не выпускается:

- конфиг Башкортостана в общем эталоне, поиск открытых президиумов;
- расширенный импорт КСОЮ, проверка неоднозначных связок и очередь
  `pending_cassation_changes`;
- время конкретного суда в UI, календаре и дайджесте, метки `updated_at`
  с явным смещением;
- общий `import_gateway.js`, расширенный список ручных импортов;
- региональные токены прогресса и расширение проб источников.

Часть этого кода уже может присутствовать в собственной ветке Башкортостана.
Дата документа не доказывает перенос пакета в остальные территории: наличие
реализации и её настроек проверяется в конкретном checkout.

Для независимости документации от несовпадающих версий кода 200 числовых
ссылок технических глав заменены ссылками на файлы с именами функций.
Отсутствующие в опубликованном эталоне файлы названы как локальная разработка,
без неработающих ссылок. Архивы инструкций сохраняют исторические ссылки.

Проверка точного документационного состава поверх опубликованного кода:
`refresh_doc_anchors.py` — 0 исправлений; три теста документации прошли,
локальные ссылки проверены. Markdown проходит `git diff --check`;
справочный CSV сохранён побайтно в исходном UTF-8 BOM/CRLF. Для проверки
всего diff с этим форматом используется `git -c core.whitespace=cr-at-eol
diff --check` — без ошибок. CI-тесты
в `tests.yml` игнорируют изменения `**.md` и `docs/**`; полный suite или
доставка для этого документационного выпуска не запускались.
