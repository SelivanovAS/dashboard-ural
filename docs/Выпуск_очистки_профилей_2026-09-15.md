# Выпуск очистки профилей — 15.09.2026

## Результат

Обновлены Workers ХМАО, Урала и Башкортостана. Каждая указанная версия
обслуживает 100% запросов; версия, содержимое JS и настройки прочитаны
через Cloudflare API после выкладки.

| Территория | Worker | Version ID | Время выкладки UTC |
|---|---|---|---|
| ХМАО | `court-monitor-trigger` | `ae4fde71-8580-45db-8708-79b1918f97b8` | 2026-09-15T05:56:22.804775Z |
| Урал | `court-monitor-ural` | `32d0b4a9-5ba2-4cd9-89dd-603feb6cb8f7` | 2026-09-15T05:58:09.48978Z |
| Башкортостан | `court-monitor-bashkortostan` | `82b437f1-321c-4924-ba21-b17786fb21e4` | 2026-09-15T05:59:06.653317Z |

Во всех трёх Workers включены `PROFILE_CLEANUP_ENABLED="1"` и единственный
cron `17 21 * * *` — ежедневно 02:17 Екатеринбург. `CRON_UTC=""`,
`IMPORT_EXECUTOR="vps"`, существующие переменные, имена секретов и привязки
KV сохранены. В Башкортостане сохранены календарные настройки и действующий
`IMPORT_GATEWAY_ORIGIN`; отдельный Worker api2 не перевыкладывался.

Сроки: пустой профиль без календаря и push — 7 дней без использования;
профиль с делами или календарём без push — 30 дней; затем 7 дней восстановления.
Связанная push-подписка защищает профиль. Учёт активности не переписывает
watchlist. [Подробные правила](technical/09-cloudflare-worker.md#очистка-неактивных-профилей-15092026).

## Первый проход

Следующий слот после выкладки — **16.09.2026 в 02:17 Екатеринбург**.
На момент проверки он ещё не выполнялся. Для старых профилей без отметки
активности первый проход начнёт полный новый срок; старые даты создания
не используются как основание немедленного удаления.

## Состав и воспроизводимость

Выкладка подготовлена отдельно от грязного рабочего каталога. Базовые сборки
побайтно совпали с прежним кодом Cloudflare. Поверх них применены только
модуль `profile_lifecycle.js`, вызовы учёта/восстановления в `worker.js`,
отображение в `admin_page.js`, отдельный cron и флаг. Другие локальные
изменения календарного времени, импортов и интерфейса в выпуск не включались.

Исходные Worker-файлы ХМАО/Урала соответствуют коммиту `141460a5`,
Башкортостана — `a57795a1`. Полные исходники сборок, патчи относительно
предыдущего выпуска, конфиги и результаты проверок сохранены локально в
`/private/tmp/court-profile-release-20260915/` (каталоги `hmao`, `ural`,
`bashkortostan`; файлы `*-source.patch`, `verified-*.json`). Это временный
каталог, не долговременное хранилище выпуска.

SHA-256 загруженного JS, подтверждённый повторным скачиванием:

- ХМАО: `b31bb96bb85ad8b792c3d8d0f1fa6b76bbe97fcc065e9e705bd117f5626d1fdd`.
- Урал: `b31bb96bb85ad8b792c3d8d0f1fa6b76bbe97fcc065e9e705bd117f5626d1fdd`.
- Башкортостан: `5c4f971b8fae70b334948bf1480421a4e50a23de550b81c84be8caa6b8233162`.

**Граница Git:** деплой Workers выполнен до фиксации исходников. Код,
регрессионные тесты и документация очистки включены в один коммит вместе с
этим отчётом. Другие незавершённые изменения рабочего эталона в него не входят.
Общий код переносится в региональные ветки через merge; cron и флаг очистки
переносятся с сохранением собственных KV, адресов, VAPID, календаря и шлюза.
Исходники Worker каждого форка при публикации сверяются с уже выложенной
сборкой. Эта Git-публикация не требует повторного деплоя и не обновляет VPS.

## Проверки

- Перед выкладкой по **206 тестов passed** отдельно на исходниках каждого
  регионального выпуска: профили, календарь, админка, очередь импортов,
  восстановление, граничные сроки и отделение cron от парсинга/рассылок.
- В рабочем эталоне до выделения выпуска полный suite: **2970 passed**.
- Перед фиксацией исходников полный suite на изолированной версии эталона:
  **2807 passed**. Этот набор не включает тесты других незавершённых изменений.
- После выкладки код каждого Worker побайтно совпал с подготовленной сборкой;
  флаг, расписание, прежние переменные и KV проверены через API.
- Во всех территориях проверены отказ восстановления без авторизации (401),
  неизвестный профиль и недействительный календарный токен (404), без
  создания или изменения пользовательских профилей.
- В Урале и Башкортостане авторизованные чтения `/admin/data`, HTML админки и
  полной очереди импортов вернули 200; новые поля жизненного цикла и интерфейс
  присутствуют. В ХМАО владелец через HTTP не проверен: местный административный
  ключ не подошёл ещё при исходном аудите. Доступ Cloudflare подтвердил код и KV.
- Плановую очистку, восстановление реального профиля, загрузку дампа и
  доставку сообщений в production принудительно не запускали.

## Сверка записей KV до и после

Снимки всех ключей `profile:*`, `sub:*`, `calfeed:*`, `profile-activity:*`
и `profile-removed:*` сняты через Cloudflare API до и после выпуска.
Значения и существующие TTL совпали полностью; удалений и созданий
профилей при выкладке не было. Секретные UUID, токены календарей и
адреса push в этот отчёт не включены.

| Территория | Профили | Push-подписки | Индексы календаря |
|---|---:|---:|---:|
| ХМАО | 14 | 13 | 7 |
| Урал | 2 | 9 | 0 |
| Башкортостан | 0 | 2 | 0 |

Служебных записей активности и удаления в обоих снимках ещё нет.
