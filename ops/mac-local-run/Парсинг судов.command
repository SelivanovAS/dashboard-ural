#!/bin/bash
# Ярлык оставлен для привычки и старых копий на рабочем столе: всё управление
# переехало в «СберСуд-пульт.command» (меню: парсинг · дампы · живой лог ·
# проверка). Этот файл просто открывает пульт.
exec /bin/bash "$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)/СберСуд-пульт.command"
