#!/bin/sh
# Заглушка netstat для VPS: BSD-вызов `netstat -rn -f inet` из
# cm_in_sber_network на Linux либо падает (флаг -f), либо отдаёт формат без
# слова «default» — пустой вывод здесь детерминированно означает «не в сети
# Сбера», и боевые скрипты идут веткой --anywhere. Настоящий net-tools на VPS
# НЕ ставить: эта заглушка стоит первой в PATH и делает поведение явным.
exit 0
